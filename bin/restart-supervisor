#!/bin/bash


supervisorctl "$@" stop all 2>&1 > /dev/null \
    && echo Stopping any running supervisord services
supervisorctl "$@" shutdown 2>&1 >/dev/null \
    && echo Shutdown supervisord
supervisord "$@" && echo launched supervisord
