#! /bin/bash

# ln improved, resolves relative paths and makes
# link folder exist before hand

function help_me() {
    echo -e "Usage: gcop <link-location> <target-location> <other ln args>\n"
}

if [[ $# -lt 2 ]]; then
	  echo "Not enough arguments passed"
	  help_me
	  return
fi

mkdir -p "$1"
